# SMB 路线图

本文件记录已经讨论、但不属于当前版本范围的方向。它不是承诺日期；只有能明显改善长期使用体验并具备可靠数据来源时才进入实施。

## 已在 2.6.0 交付

- **SMB 主动提醒**：每天（默认 09:00）有待确认续费或临近计划时，SMB 通过 OpenClaw Gateway hook 触发 OpenClaw，由它读取 MCP 详情并发 Telegram 提醒；没有待办时不打扰。
- **MCP 安全标注**：所有工具声明只读/破坏性提示，OpenClaw 可以放行只读查询。
- **手机浏览器底栏**：底栏不再被挤出屏幕，事项角标回到图标上。

## 已在 2.5.4 交付

- **应用内确认与撤销**：全站不再弹浏览器原生确认框；移入回收站即时生效并提供 6 秒「撤销」；保存后不再等待全站数据刷新。

## 已在 2.5.3 交付

- **洞察同进度文案**：环比卡片写清「较上月同期」和实际上期日期；分类明细显示上期金额，避免把 +200% 误当成整月或占比。

## 已在 2.5.2 交付

- **检查并刷新同步本机服务**：托管启动的生产进程在磁盘构建更新后可由设置按钮优雅重启，再刷新网页。

## 已在 2.5.1 交付

- **体检核对稳定**：预算超支/接近上限的核对指纹不再随每笔记账金额变化；已核对项不再占体检列表。
- **资金流水倒序**：最近资金流水按发生日从新到旧，固定窗口滚动加载。
- **计划分栏**：未完成 / 已结束隔离，电脑左右分栏；完成与删除改用应用内确认。
- **宽屏字号**：分类明细与 ≥1200px 字阶略放大。

## 已在 2.5.0 交付

- **一次性计划**：事项第三页签记录预购尾款等非订阅跟进；录入不绑账户，有金额完成时才入账。
- **计划提醒**：页签角标、底栏事项角标、洞察卡片和体检 `plan_due`。
- **OpenClaw 计划工具**：direct 模式增删改完成，confirm 模式只读。

## 已在 2.4.1 交付

- **资金启用基线**：启用瞬间把已有账目写入 `funds_baseline_transactions`，同日旧账不再被误报为缺少账户。
- **缺少账户的聚合核对**：同月多笔缺少账户合并成一条体检提示，可展开核对并批量指定人民币账户，提交前显示余额影响。
- **外币账户双金额**：账户可为 CNY/USD/USDT；账本金额始终是人民币，外币账户必须另填实际金额。

## 后续资金维护

- **资金一致性回归**：继续补齐补录旧日期账目、OpenClaw 外币写入和恢复演练的组合测试。

## 优先调研

- **账单文件导入**：先研究微信、支付宝和银行导出的 CSV/Excel 格式、去重规则、预览和可撤销导入。
- **账单导入后的余额核对**：只有导入能提供稳定的交易身份和撤销机制时，才与现有资金流水联动。
- **逐字段账目历史**：在跨设备修改频率明显提高后，评估可读的变更历史和回滚粒度。
- **网页自然语言查询**：仅在现有筛选和体检无法满足查询时考虑；程序仍负责数字。
- **AI 备注逐次授权**：当前普通分析默认不向外部模型发送备注。只有在再次明确确认敏感正文外发风险后，才评估不持久化的单次授权开关。

## 需要独立大版本

- 信用卡、负债、投资、部分退款和完整的导入对账体系。
- 通过个人可用、只读、可撤销的官方授权聚合实时资产。
- 独立财务事件时间线或更复杂的导入工作流。

## 明确不做

- 不保存支付平台密码、Cookie 或验证码。
- 不模拟登录、抓取个人余额页面或依赖非官方私有接口。
- 不自动追溯资金追踪启用前的旧账，也不把待收借款或投资计入可用资金。
- 不把预算变成阻止记账的限制，也不自动结转未用金额。
